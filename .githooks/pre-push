#!/usr/bin/env bash
# Runs the confidentiality check before allowing a push.
# Enable with: git config core.hooksPath .githooks
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
exec bash "$repo_root/scripts/check-confidential.sh"
